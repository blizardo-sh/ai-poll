{
  "name": "lesson-one-poll",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/app.js --bundle --minify --outfile=app.js",
    "test:rules": "node --test tests/rules.test.mjs"
  },
  "dependencies": {
    "firebase": "^12.18.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "esbuild": "^0.28.2",
    "playwright": "^1.63.0",
    "qrcode": "^1.5.4"
  }
}
